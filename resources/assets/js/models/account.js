export class User {
    constructor({ id, username, url, gender, description }) {
        this.id = id
        this.username = username
        this.url = url
        this.gender = gender
        this.description = description
    }
}
